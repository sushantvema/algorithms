# Bucket Sort

Bucket sort divides elements into various groups or buckets.

These are formed by uniformly distributing the elements. Once elements are
divided into buckets, they can be sorted using any other sorting algorithm.

Then the sorted elements are gathered together in an ordered fashion.
