# 3-Way Merge Sort

This is just merge sort but instead of partitioning into two halves, we start
with splitting the array into three equal parts and applying recursion.

This potentially reduces recursion depth and might improve efficiency.
