# Cycle Sort

Cycle sort is an in-place unstable sorting algorithm that is useful when sorting
arrays containing elements with a small range of values. 

Optimal in terms of several memory writes. 

I've never actually seen this before, so will need to do more research here. 
