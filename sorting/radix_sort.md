# Radix Sort

Radix sort is a linear sorting algorithm which sorts elements by processing them
digit by digit.

This is efficient for integers or strings with fixed-size keys.

This algorithm distributes elements into buckets based on each digit's value. 

I don't fully understand this algorithm yet so I need to look at some
implementations
