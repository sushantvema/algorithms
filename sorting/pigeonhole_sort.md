# Pigeonhole Sort

Pigeonhole sort is a sorting algorithm that is suitable for sorting lists of
elements where number of elements and number of possible key values are
approximately the same.

Requirements O(n+range) time where n is number of elements in input array and
range is number of possible values in array

Not a lot of intuition here
